# OOPSBannerApp_UC4
This use case extends the OOPS Banner App by improving the implementation from UC3. Instead of using multiple individual System.out.println() statements, the banner lines are stored inside a String[] array and printed using an enhanced for-loop.  
